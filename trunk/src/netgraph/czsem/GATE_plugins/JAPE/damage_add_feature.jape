Phase:phase1
Input: damage   
Options: control = appelt

Rule: damage_add_learninig_feature
Priority: 20
(
{damage}
):damage_tmp
-->
{
	gate.AnnotationSet binding_as = (gate.AnnotationSet) bindings.get("damage_tmp");
	gate.Annotation annot = binding_as.iterator().next();
	annot.getFeatures().put("rule", "damage_add_learninig_feature");			
}
