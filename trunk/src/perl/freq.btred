#!btred -TNe freq_lemma()

use vars qw($this $root);

sub freq_lemma()
{
	if ($this->{t_lemma} eq "")
	{
#		print $this->{t_lemma} . "\n"
	}
	else
	{
		print $this->{t_lemma} . "\n"
	}
}

sub freq_verb()
{
	if ($this->{gram}{sempos} eq "v")
	{
		print $this->{t_lemma} . "\n"
	}
}